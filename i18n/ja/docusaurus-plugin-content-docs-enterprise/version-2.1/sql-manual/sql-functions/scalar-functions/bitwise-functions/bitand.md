---
{
  "title": "BITAND",
  "description": "ビット単位のAND演算を実行するために使用されます。ビット単位のAND演算は、2つの整数の各ビットを比較します。",
  "language": "ja"
}
---
## 説明
ビット単位のAND演算を実行するために使用されます。ビット単位のAND演算は、2つの整数の各ビットを比較します。対応する両方のバイナリビットが1の場合のみ結果は1となり、それ以外の場合は0となります。

整数の範囲：TINYINT、SMALLINT、INT、BIGINT、LARGEINT

## 構文

```sql
BITAND( <lhs>, <rhs>)
```
## パラメータ
| parameter | description  |
|-----------|--------------|
| `<lhs>`   | ビット演算ANDに使用する最初の数値 |
| `<rhs>`   | ビット演算ANDに使用する2番目の数値 |

## 戻り値

2つの整数のAND演算の結果を返します。


## 例

```sql
select BITAND(3,5), BITAND(4,7);
```
```text
+---------+---------+
| (3 & 5) | (4 & 7) |
+---------+---------+
|       1 |       4 |
+---------+---------+

```
