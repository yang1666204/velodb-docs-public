---
{
  "title": "MAP_VALUES",
  "description": "指定されたマップの値を対応する型のARRAYに抽出します",
  "language": "ja"
}
---
## 説明

指定された`map`の値を対応する型の`ARRAY`に抽出します

## 構文

```sql
MAP_VALUES(<map>)
```
## パラメータ

| Parameter | デスクリプション |
| -- | -- |
| `<map>` | 入力するmapコンテンツ |

## Return Value

指定された`map`の値を抽出し、対応する型の`ARRAY`にします

## Example

```sql
select map_values(map()),map_values(map(1, "100", 0.1, 2));
```
```text
+-------------------+---------------------------------------------------------------------------------------------------+
| map_values(map()) | map_values(map(cast(1 as DECIMALV3(2, 1)), '100', cast(0.1 as DECIMALV3(2, 1)), cast(2 as TEXT))) |
+-------------------+---------------------------------------------------------------------------------------------------+
| []                | ["100", "2"]                                                                                      |
+-------------------+---------------------------------------------------------------------------------------------------+
```
