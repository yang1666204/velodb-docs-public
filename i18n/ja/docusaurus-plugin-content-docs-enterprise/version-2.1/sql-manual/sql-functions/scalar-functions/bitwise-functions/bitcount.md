---
{
  "title": "BIT_COUNT",
  "description": "整数値のバイナリ表現における1ビットの数を返すために使用されます。",
  "language": "ja"
}
---
## Description
整数値のバイナリ表現における1ビットの数を返すために使用されます。この関数は、バイナリ表現において整数の「アクティブ」ビット数を素早くカウントするために使用でき、通常はデータ分散の分析や特定のビット演算の実行に使用されます。

## Syntax

```sql
BIT_COUNT( <x>)
```
## パラメータ
| parameter | description                                                     |
|-----------|-----------------------------------------------------------------|
| `<x>`     | 整数xのバイナリ表現における1の個数をカウントします。整数型は次のいずれかです：TINYINT、SMALLINT、INT、BIGINT、LARGEINT |

## 戻り値

`<x>`のバイナリ表現における1の個数を返します

## 例

```sql
select BIT_COUNT(8), BIT_COUNT(-1);
```
```text
+--------------+---------------+
| bit_count(8) | bit_count(-1) |
+--------------+---------------+
|            1 |             8 |
+--------------+---------------+
```
