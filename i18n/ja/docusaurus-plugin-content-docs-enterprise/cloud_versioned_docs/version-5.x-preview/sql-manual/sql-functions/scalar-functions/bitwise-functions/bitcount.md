---
{
  "title": "BIT_COUNT",
  "description": "整数値のバイナリ表現における1ビットの数を返します。",
  "language": "ja"
}
---
## 説明

整数値のバイナリ表現における1ビットの数を返します。この関数は、整数のバイナリ表現における「アクティブ」ビットを素早くカウントするために使用でき、データ分布の解析や特定のビット演算の実行に有用です。

## 構文

```sql
BIT_COUNT(<x>)
```
## パラメータ
- `<x>`: バイナリ表現で1ビットがカウントされる整数。サポートされる型: TINYINT, SMALLINT, INT, BIGINT, LARGEINT。

## 戻り値

`<x>`のバイナリ表現における1ビットの数を返します。

## 例
1. 例1

    ```sql
    select BIT_COUNT(0), BIT_COUNT(8), BIT_COUNT(-1);
    ```
    ```text
    +--------------+--------------+---------------+
    | BIT_COUNT(0) | BIT_COUNT(8) | BIT_COUNT(-1) |
    +--------------+--------------+---------------+
    |            0 |            1 |             8 |
    +--------------+--------------+---------------+
    ```
2. NULL引数

    ```sql
    select BIT_COUNT(NULL);
    ```
    ```text
    +-----------------+
    | BIT_COUNT(NULL) |
    +-----------------+
    |            NULL |
    +-----------------+
    ```
