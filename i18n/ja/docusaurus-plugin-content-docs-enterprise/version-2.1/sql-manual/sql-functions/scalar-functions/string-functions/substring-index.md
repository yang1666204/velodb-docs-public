---
{
  "title": "SUBSTRING_INDEX",
  "description": "SUBSTRINGINDEX関数は、指定された区切り文字と出現回数に基づいて文字列から部分文字列を抽出するために使用されます。",
  "language": "ja"
}
---
## 説明

SUBSTRING_INDEX関数は、指定された区切り文字と出現回数に基づいて文字列から部分文字列を抽出するために使用されます。この関数は左からまたは右からのカウントをサポートします。

## 構文

```sql
SUBSTRING_INDEX(<content>, <delimiter>, <field>)
```
## パラメータ
| Parameter | デスクリプション                                                                                                     |
| --------- | --------------------------------------------------------------------------------------------------------------- |
| `<content>` | 抽出元となる文字列。型: VARCHAR                                                                  |
| `<delimiter>` | 区切り文字列、大文字小文字を区別し、マルチバイト対応。型: VARCHAR                                         |
| `<field>` | 区切り文字の出現回数。正の数は左から数え、負の数は右から数える。型: INT |

## Return Value

抽出された部分文字列を表すVARCHAR型を返します。

特殊なケース:
- field > 0 の場合、左からfield番目の区切り文字より前の部分文字列を返す
- field < 0 の場合、右から|field|番目の区切り文字より後の部分文字列を返す
- field = 0 の場合、contentがNULLでない時は空文字列を返し、contentがNULLの時はNULLを返す
- いずれかのパラメータがNULLの場合、NULLを返す

## Examples

1. 左から最初のスペースより前の内容を抽出

```sql
SELECT substring_index('hello world', ' ', 1);
```
```text
+----------------------------------------+
| substring_index('hello world', ' ', 1) |
+----------------------------------------+
| hello                                  |
+----------------------------------------+
```
2. 左側からすべてのコンテンツを抽出する（区切り文字の数が実際の出現回数より多い場合）

```sql
SELECT substring_index('hello world', ' ', 2);
```
```text
+----------------------------------------+
| substring_index('hello world', ' ', 2) |
+----------------------------------------+
| hello world                            |
+----------------------------------------+
```
3. 右端から最後のスペース以降の内容を抽出する

```sql
SELECT substring_index('hello world', ' ', -1);
```
```text
+-----------------------------------------+
| substring_index('hello world', ' ', -1) |
+-----------------------------------------+
| world                                   |
+-----------------------------------------+
```
4. fieldが0の場合

```sql
SELECT substring_index('hello world', ' ', 0);
```
```text
+----------------------------------------+
| substring_index('hello world', ' ', 0) |
+----------------------------------------+
|                                        |
+----------------------------------------+
```
