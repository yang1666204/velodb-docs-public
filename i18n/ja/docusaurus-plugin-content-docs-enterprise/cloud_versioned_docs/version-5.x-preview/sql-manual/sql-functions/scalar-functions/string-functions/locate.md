---
{
  "title": "LOCATE",
  "description": "LOCATE関数は、文字列str内で部分文字列substrが最初に出現する位置を返します（カウントは1から開始されます）。",
  "language": "ja"
}
---
## 説明

LOCATE関数は、文字列strの中で部分文字列substrが最初に出現する位置を返します（カウントは1から開始）。オプションの第3パラメータposが指定された場合、文字列str内の指定された位置から検索を開始します。これは文字列マッチングや位置検索によく使用されるMySQL互換の関数です。

## 構文

```sql
LOCATE(<substr>, <str> [, <pos>])
```
## パラメータ

| Parameter | Description |
|----------|-----------------|
| `<substr>` | 検索対象の部分文字列。型: VARCHAR |
| `<str>`    | 検索元の文字列。型: VARCHAR |
| `<pos>`    | オプションパラメータ、検索開始位置（1から数える）。型: INT |

## 戻り値

INT型を返し、strの中でsubstrが最初に出現する位置を表す（1から数える）。

検索ルール:
- 位置のカウントは1から始まる（0からではない）
- 最初にマッチした位置を返す
- posが指定された場合、その位置から検索を開始するが、返される位置は文字列の先頭からの絶対位置である
- 検索は大文字小文字を区別する

特殊ケース:
- マッチするものが見つからない場合、0を返す
- いずれかのパラメータがNULLの場合、NULLを返す
- substrが空文字列の場合、1を返す（posが指定され、pos > 1の場合はposの値を返す）
- strが空でsubstrが空でない場合、0を返す
- posが1未満の場合、0を返す
- posがstrの長さより大きい場合、0を返す

## 例

1. 基本的な検索

```sql
SELECT LOCATE('bar', 'foobarbar'), LOCATE('xbar', 'foobar'), LOCATE('bar', 'foobarbar', 5);
```
```text
+----------------------------+--------------------------+-------------------------------+
| LOCATE('bar', 'foobarbar') | LOCATE('xbar', 'foobar') | LOCATE('bar', 'foobarbar', 5) |
+----------------------------+--------------------------+-------------------------------+
|                          4 |                        0 |                             7 |
+----------------------------+--------------------------+-------------------------------+
```
2. 最初の文字を見つける

```sql
SELECT LOCATE('f', 'foobar'), LOCATE('r', 'foobar');
```
```text
+-----------------------+-----------------------+
| LOCATE('f', 'foobar') | LOCATE('r', 'foobar') |
+-----------------------+-----------------------+
|                     1 |                     6 |
+-----------------------+-----------------------+
```
3. マッチが見つかりませんでした

```sql
SELECT LOCATE('xyz', 'foobar'), LOCATE('FOO', 'foobar');
```
```text
+-------------------------+-------------------------+
| LOCATE('xyz', 'foobar') | LOCATE('FOO', 'foobar') |
+-------------------------+-------------------------+
|                       0 |                       0 |
+-------------------------+-------------------------+
```
