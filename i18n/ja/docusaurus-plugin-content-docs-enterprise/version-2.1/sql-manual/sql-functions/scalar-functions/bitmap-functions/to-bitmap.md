---
{
  "title": "TO_BITMAP",
  "description": "符号なしbigintをBitmapに変換します。",
  "language": "ja"
}
---
## 概要

符号なしbigintをBitmapに変換します。

入力は0から18446744073709551615の範囲の値を持つ符号なしbigintで、出力はその要素を含むBitmapです。

## 構文

```sql
TO_BITMAP(<expr>)
```
## パラメータ

| パラメータ | 説明                                        |
|-----------|----------------------------------------------------|
| `<expr>`  | 0から18446744073709551615の範囲の符号なしbigint |

## 戻り値

対応するbigintを含むBitmap。  
入力値が指定された範囲内にない場合は`NULL`を返します。

## 例

整数をBitmapに変換し、Bitmap内の要素数をカウントするには：

```sql
select bitmap_count(to_bitmap(10));
```
結果は以下のようになります：

```text
+-----------------------------+
| bitmap_count(to_bitmap(10)) |
+-----------------------------+
|                           1 |
+-----------------------------+
```
負の整数をBitmapに変換する場合（これは有効範囲外です）、それを文字列に変換するには：

```sql
select bitmap_to_string(to_bitmap(-1));
```
結果は以下のようになります：

```text
+---------------------------------+
| bitmap_to_string(to_bitmap(-1)) |
+---------------------------------+
|                                 |
+---------------------------------+
```
