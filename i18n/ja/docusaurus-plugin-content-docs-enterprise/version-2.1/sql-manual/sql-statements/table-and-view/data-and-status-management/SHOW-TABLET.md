---
{
  "title": "SHOW TABLET",
  "description": "この文は特定のタブレットの詳細を表示するために使用されます（管理者のみ）。",
  "language": "ja"
}
---
## 説明

この文は特定のタブレットの詳細を表示するために使用されます（管理者のみ）。

## 構文

```sql
SHOW TABLET <tablet_id>;
```
## 必須パラメータ

**1. `<tablet_id>`**

> 情報を表示する特定のタブレットのIDです。

## 戻り値

`SHOW TABLET <tablet_id>`を使用した場合、以下の列が返されます：

| Column        | DataType | Note                                                                   |
|---------------|----------|------------------------------------------------------------------------|
| DbName        | String   | タブレットを含むデータベースの名前。                     |
| TableName     | String   | タブレットを含むTableの名前。                        |
| PartitionName | String   | タブレットを含むパーティションの名前。                    |
| IndexName     | String   | タブレットを含むインデックスの名前。                        |
| DbId          | Int      | データベースのID。                                                |
| TableId       | Int      | TableのID。                                               |
| PartitionId   | Int      | パーティションのID。                                               |
| IndexId       | Int      | インデックスのID。                                                   |
| IsSync        | Boolean  | タブレットがそのレプリカと同期されているかどうか。                       |
| Order         | Int      | タブレットの順序。                                               |
| QueryHits     | Int      | このタブレットのクエリヒット数。                               |
| DetailCmd     | String   | タブレットの詳細情報を取得するためのコマンド。         |

## アクセス制御要件

このSQLコマンドを実行するユーザーは、最低限以下の権限を持つ必要があります：

| Privilege  | Object   | 注釈                                                                                                                            |
|:-----------|:---------|:---------------------------------------------------------------------------------------------------------------------------------|
| Admin_priv | Database | Table、パーティション、システムレベルのコマンドの管理を含む、データベースでの管理操作の実行に必要です。 |

## 例

特定のタブレットの詳細を表示：

```sql
SHOW TABLET 10145;
```
```text
+--------+-----------+---------------+-----------+-------+---------+-------------+---------+--------+-------+-----------+------------------------------------------------------------+
| DbName | TableName | PartitionName | IndexName | DbId  | TableId | PartitionId | IndexId | IsSync | Order | QueryHits | DetailCmd                                                  |
+--------+-----------+---------------+-----------+-------+---------+-------------+---------+--------+-------+-----------+------------------------------------------------------------+
| test   | sell_user | sell_user     | sell_user | 10103 | 10143   | 10142       | 10144   | true   | 0     | 0         | SHOW PROC '/dbs/10103/10143/partitions/10142/10144/10145'; |
+--------+-----------+---------------+-----------+-------+---------+-------------+---------+--------+-------+-----------+------------------------------------------------------------+
```
