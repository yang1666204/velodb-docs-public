---
{
  "title": "FIND_IN_SET",
  "description": "カンマ区切りの文字列リスト strlist 内で、文字列 str が最初に出現する位置を返します（カウントは1から開始されます）。",
  "language": "ja"
}
---
## 説明

カンマ区切りの文字列リストstrlist内で、文字列strが最初に出現する位置を返します（カウントは1から始まります）。この関数はMySQL互換モードで実装されており、カンマ区切りの値リスト内で特定の文字列を検索するために使用されます。

## 構文

```sql
FIND_IN_SET(<str>, <strlist>)
```
## パラメータ

| Parameter | Description |
|-------------|----------|
| `<str>`     | 検索対象の文字列。型: VARCHAR |
| `<strlist>` | str を検索するカンマ区切りの文字列リスト。型: VARCHAR |

## 戻り値

INT 型を返し、strlist 内での str の位置を表します（カウントは 1 から開始）。

検索ルール:
- 完全一致: str が strlist 内の部分文字列と完全に一致した場合のみ位置を返します
- 位置のカウントは 1 から開始します
- 最初に一致した位置を返します

特殊ケース:
- str が空文字列の場合、0 を返します
- strlist が空文字列の場合、0 を返します
- 一致が見つからない場合、0 を返します
- いずれかのパラメータが NULL の場合、NULL を返します
- str にカンマが含まれている場合、正しくマッチできません（カンマは区切り文字のため）
- マッチングは大文字小文字を区別します

## 例

1. 基本的な検索

```sql
SELECT FIND_IN_SET('b', 'a,b,c');
```
```text
+---------------------------+
| FIND_IN_SET('b', 'a,b,c') |
+---------------------------+
|                         2 |
+---------------------------+
```
2. 最初の要素を見つける

```sql
SELECT FIND_IN_SET('apple', 'apple,banana,cherry');
```
```text
+------------------------------------------+
| FIND_IN_SET('apple', 'apple,banana,cherry') |
+------------------------------------------+
|                                        1 |
+------------------------------------------+
```
3. 最後の要素を見つける

```sql
SELECT FIND_IN_SET('cherry', 'apple,banana,cherry');
```
```text
+-------------------------------------------+
| FIND_IN_SET('cherry', 'apple,banana,cherry') |
+-------------------------------------------+
|                                         3 |
+-------------------------------------------+
```
4. 一致するものが見つかりません

```sql
SELECT FIND_IN_SET('orange', 'apple,banana,cherry');
```
```text
+--------------------------------------------+
| FIND_IN_SET('orange', 'apple,banana,cherry') |
+--------------------------------------------+
|                                          0 |
+--------------------------------------------+
```
5. NULL値の処理

```sql
SELECT FIND_IN_SET(NULL, 'a,b,c'), FIND_IN_SET('b', NULL);
```
```text
+---------------------------+-------------------------+
| FIND_IN_SET(NULL, 'a,b,c') | FIND_IN_SET('b', NULL) |
+---------------------------+-------------------------+
|                      NULL |                    NULL |
+---------------------------+-------------------------+
```
6. 空文字列の処理

```sql
SELECT FIND_IN_SET('', 'a,b,c'), FIND_IN_SET('a', '');
```
```text
+-------------------------+-----------------------+
| FIND_IN_SET('', 'a,b,c') | FIND_IN_SET('a', '') |
+-------------------------+-----------------------+
|                       0 |                     0 |
+-------------------------+-----------------------+
```
7. カンマを含む文字列（正しくマッチできません）

```sql
SELECT FIND_IN_SET('a,b', 'a,b,c,d');
```
```text
+------------------------------+
| FIND_IN_SET('a,b', 'a,b,c,d') |
+------------------------------+
|                            0 |
+------------------------------+
```
8. 大文字小文字を区別したマッチング

```sql
SELECT FIND_IN_SET('B', 'a,b,c'), FIND_IN_SET('b', 'A,B,C');
```
```text
+---------------------------+---------------------------+
| FIND_IN_SET('B', 'a,b,c') | FIND_IN_SET('b', 'A,B,C') |
+---------------------------+---------------------------+
|                         0 |                         0 |
+---------------------------+---------------------------+
```
9. 部分一致は成功しません

```sql
SELECT FIND_IN_SET('ap', 'apple,banana,cherry');
```
```text
+---------------------------------------+
| FIND_IN_SET('ap', 'apple,banana,cherry') |
+---------------------------------------+
|                                     0 |
+---------------------------------------+
```
10. 数値文字列検索

```sql
SELECT FIND_IN_SET('2', '1,2,3,10,20');
```
```text
+--------------------------------+
| FIND_IN_SET('2', '1,2,3,10,20') |
+--------------------------------+
|                              2 |
+--------------------------------+
```
