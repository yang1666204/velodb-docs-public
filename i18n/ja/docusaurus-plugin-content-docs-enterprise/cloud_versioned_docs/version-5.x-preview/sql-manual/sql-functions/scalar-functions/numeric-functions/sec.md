---
{
  "title": "SEC",
  "description": "xの正割を返します。xはラジアン単位の値で、入力と出力はdoubleのみサポートされています。入力値がnullの場合、null値を返します。",
  "language": "ja"
}
---
## 説明

xの正割を返します。ここでxはラジアン単位の値で、入力と出力はdoubleのみサポートされています。入力がnull値の場合、null値を返します。

## 構文

```sql
SEC(<x>)
```
## Parameters

| Parameter | Description |
| -- | -- |
| `<x>` | secantを計算する値 |

## Return Value

xのsecantを表すDouble型の値を返します。

## Special Cases
- `x`がNaNの場合、NaNを返します
- `x`が正または負の無限大の場合、NaNを返します
- `x`がNULLの場合、NULLを返します

## Example

```sql
select sec(1),sec(2),sec(1000);
```
```text
+--------------------+--------------------+--------------------+
| sec(1)             | sec(2)             | sec(1000)          |
+--------------------+--------------------+--------------------+
| 1.8508157176809255 | -2.402997961722381 | 1.7781600385912715 |
+--------------------+--------------------+--------------------+
```
Input null値。

```sql
select sec(null);
```
```text
+--------------------+
| sec(null)          |
+--------------------+
|      NULL          |
+--------------------+
```
```sql
select sec(cast('nan' as double));
```
```text
+---------------------------+
| sec(cast('nan' AS DOUBLE))|
+---------------------------+
| NaN                       |
+---------------------------+
```
```sql
select sec(cast('inf' as double));
```
```text
+---------------------------+
| sec(cast('inf' AS DOUBLE))|
+---------------------------+
| NaN                       |
+---------------------------+
```
```sql
select sec(cast('-inf' as double));
```
```text
+----------------------------+
| sec(cast('-inf' AS DOUBLE))|
+----------------------------+
| NaN                        |
+----------------------------+
```
