---
{
  "title": "RANDOM",
  "description": "0と1の間のランダムな数値を返すか、パラメータに応じて必要なランダムな数値を返します。",
  "language": "ja"
}
---
## 説明

0と1の間のランダムな数値を返すか、またはパラメータに応じて必要なランダムな数値を返します。

- 注意：すべてのパラメータは定数である必要があります。

## エイリアス

- RAND

## 構文

```sql
RANDOM() -- Generates a random number between 0 and 1

RANDOM(<seed>) -- Generates a fixed sequence of random numbers between 0 and 1 based on the seed value

RANDOM(<a> , <b>) -- Generates a random number between a and b
```
## パラメータ

| Parameter | Description |
|-----------|------------|
| `<seed>` | 乱数ジェネレータのシード値。0と1の間の固定された乱数のシーケンスを返します。 |
| `<a>` | 乱数の下限。 |
| `<b>` | 乱数の上限。下限より小さくなければなりません。 |

## 戻り値

- パラメータが渡されない場合: 0と1の間の乱数を返します。

- 単一のパラメータseedが渡される場合: 0と1の間の固定された乱数のシーケンスを返します。

- 2つのパラメータaとbが渡される場合: aとbの間の乱数の整数を返します。

## 例

```sql
select random();
```
```text
+--------------------+
| random()           |
+--------------------+
| 0.8047437125910604 |
+--------------------+
```
```sql
select rand(1.2);
```
```text
+---------------------+
| rand(1)             |
+---------------------+
| 0.13387664401253274 |
+---------------------+
```
```sql
select rand(-20, -10);
```
```text
+------------------+
| random(-20, -10) |
+------------------+
|              -10 |
+------------------+
```
