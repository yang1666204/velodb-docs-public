---
{
  "title": "GREATEST",
  "description": "複数の式を比較し、それらの中で最大の値を返します。引数のいずれかがNULLの場合、関数はNULLを返します。",
  "language": "ja"
}
---
## 説明

複数の式を比較し、それらの中で最大の値を返します。引数のいずれかが`NULL`の場合、関数は`NULL`を返します。

## 構文

```sql
GREATEST(<expr> [, ...])
```
## パラメータ

| パラメータ   | 説明 |
|------------|-------------|
| `<expr>`  | 比較する式のリスト。`TINYINT`、`SMALLINT`、`INT`、`BIGINT`、`LARGEINT`、`FLOAT`、`DOUBLE`、`STRING`、`DATETIME`、`DECIMAL`型をサポートします。 |

## 戻り値

- 指定された式の中で最大の値を返します。
- 引数に`NULL`が含まれる場合、`NULL`を返します。

## 例

```sql
SELECT GREATEST(-1, 0, 5, 8);
```
```text
+-----------------------+
| GREATEST(-1, 0, 5, 8) |
+-----------------------+
|                     8 |
+-----------------------+
```
```sql
SELECT GREATEST(-1, 0, 5, NULL);
```
```text
+--------------------------+
| GREATEST(-1, 0, 5, NULL) |
+--------------------------+
| NULL                     |
+--------------------------+
```
```sql
SELECT GREATEST(6.3, 4.29, 7.6876);
```
```text
+-----------------------------+
| GREATEST(6.3, 4.29, 7.6876) |
+-----------------------------+
|                      7.6876 |
+-----------------------------+
```
```sql
SELECT GREATEST('2022-02-26 20:02:11', '2020-01-23 20:02:11', '2020-06-22 20:02:11');
```
```text
+-------------------------------------------------------------------------------+
| GREATEST('2022-02-26 20:02:11', '2020-01-23 20:02:11', '2020-06-22 20:02:11') |
+-------------------------------------------------------------------------------+
| 2022-02-26 20:02:11                                                           |
+-------------------------------------------------------------------------------+
```
