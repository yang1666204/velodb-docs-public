---
{
  "title": "TO_IPV4_OR_DEFAULT",
  "description": "この関数は、IPv4アドレスの文字列形式を受け取りIPv4型の値を返すipv4stringtonumのような関数です。",
  "language": "ja"
}
---
## 説明
この関数はipv4_string_to_numと似ており、IPv4アドレスの文字列形式を受け取り、IPv4型の値を返します。この値はipv4_string_to_numによって返される値とバイナリレベルで等しくなります。

## 構文

```sql
TO_IPV4_OR_DEFAULT(<ipv4_str>)
```
## Parameters
| Parameter | Description                                      |
|-----------|--------------------------------------------------|
| `<ipv4_str>`      | String型のIPv4アドレス |


## Return Value
IPv4型の値を返します。
- IPv4アドレスの形式が無効な場合、0.0.0.0（IPv4として0）を返します。


## Example

```sql
SELECT to_ipv4_or_default('255.255.255.255'), to_ipv4_or_default('.'), to_ipv4_or_default(NULL);
```
```text
+---------------------------------------+-------------------------+--------------------------+
| to_ipv4_or_default('255.255.255.255') | to_ipv4_or_default('.') | to_ipv4_or_default(NULL) |
+---------------------------------------+-------------------------+--------------------------+
| 255.255.255.255                       | 0.0.0.0                 | 0.0.0.0                  |
+---------------------------------------+-------------------------+--------------------------+
```
