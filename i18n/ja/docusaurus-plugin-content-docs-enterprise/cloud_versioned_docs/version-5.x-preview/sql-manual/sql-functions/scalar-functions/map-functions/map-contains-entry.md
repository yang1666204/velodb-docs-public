---
{
  "title": "MAP_CONTAINS_ENTRY",
  "description": "指定されたmapが特定のエントリ（key、value）を含むかどうかを判定します",
  "language": "ja"
}
---
## Description

指定された`map`が特定のエントリ`(key, value)`を含むかどうかを判定します

## Syntax

```sql
MAP_CONTAINS_ENTRY(<map>, <key>, <value>)
```
## パラメータ

| Parameter | Description |
| -- | -- |
| `<map>` | 入力マップのコンテンツ |
| `<key>` | 取得するキー |
| `<value>` | 取得する値 |

## 戻り値

指定された`map`が特定の`(key, value)`ペアを含むかどうかを判定します。存在する場合は1を返し、そうでなければ0を返します。`<map>`が`NULL`の場合は`NULL`を返します。

キーと値の比較には「null-safe equal」（2つの`NULL`は等しいとみなされる）を使用しており、これは標準的なSQL定義とは異なります。

## 例

```sql
select map_contains_entry(map(null, 1, 2, null), null, 1);
```
```text
+----------------------------------------------------+
| map_contains_entry(map(null, 1, 2, null), null, 1) |
+----------------------------------------------------+
|                                                  1 |
+----------------------------------------------------+
```
```sql
select map_contains_entry(map(1, '100', 0.1, '2'), 1, '100');
```
```text
+-------------------------------------------------------+
| map_contains_entry(map(1, '100', 0.1, '2'), 1, '100') |
+-------------------------------------------------------+
|                                                     1 |
+-------------------------------------------------------+
```
```sql
select map_contains_entry(map(1, '100', 0.1, '2'), 0.11, '2');
```
```text
+--------------------------------------------------------+
| map_contains_entry(map(1, '100', 0.1, '2'), 0.11, '2') |
+--------------------------------------------------------+
|                                                      0 |
+--------------------------------------------------------+
```
