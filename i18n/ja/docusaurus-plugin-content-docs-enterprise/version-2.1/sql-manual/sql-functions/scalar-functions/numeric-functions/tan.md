---
{
  "title": "TAN",
  "description": "xの正接を返します。ここでxはラジアン単位の値です。",
  "language": "ja"
}
---
## デスクリプション

xの正接を返します。xはラジアン単位の値です。

## Syntax

```sql
TAN(<x>)
```
## パラメータ

| パラメータ | 説明 |
| -- | -- |
| `<x>` | 正接を計算する値 |

## 戻り値

xの正接を返します。

## 例

```sql
select tan(0),tan(1),tan(-1);
```
```text
+------------------------+------------------------+-------------------------+
| tan(cast(0 as DOUBLE)) | tan(cast(1 as DOUBLE)) | tan(cast(-1 as DOUBLE)) |
+------------------------+------------------------+-------------------------+
|                      0 |     1.5574077246549023 |     -1.5574077246549023 |
+------------------------+------------------------+-------------------------+
```
