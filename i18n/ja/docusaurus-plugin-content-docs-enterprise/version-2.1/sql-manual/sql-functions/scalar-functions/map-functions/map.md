---
{
  "title": "MAP | Map関数",
  "description": "特定の型のMap<K, V>をキー・値ペアのセットを使用して構築します",
  "language": "ja"
}
---
# MAP

## デスクリプション

特定の型の`Map<K, V>`をキー・値ペアのセットを使用して構築します

## Syntax

```sql
MAP( <key1> , <value1> [, <key2>,<value2> ... ])
```
## パラメータ

| Parameter | デスクリプション |
| -- | -- |
| `<key>` | マップのキーを構成します。大文字小文字を区別します |
| `<value>` | マップの値を構成します |

## 戻り値

複数のキー・値ペアから構成された特定の型 `Map<K, V>` を返します

## 例

```sql
select map(1, "100", 0.1, 2),map(1, "100", 0.1, 2)[1];
```
```text
+---------------------------------------------------------------------------------------+-------------------------------------------------------------------------------------------------------------------------------+
| map(cast(1 as DECIMALV3(2, 1)), '100', cast(0.1 as DECIMALV3(2, 1)), cast(2 as TEXT)) | element_at(map(cast(1 as DECIMALV3(2, 1)), '100', cast(0.1 as DECIMALV3(2, 1)), cast(2 as TEXT)), cast(1 as DECIMALV3(2, 1))) |
+---------------------------------------------------------------------------------------+-------------------------------------------------------------------------------------------------------------------------------+
| {1.0:"100", 0.1:"2"}                                                                  | 100                                                                                                                           |
+---------------------------------------------------------------------------------------+-------------------------------------------------------------------------------------------------------------------------------+
```
