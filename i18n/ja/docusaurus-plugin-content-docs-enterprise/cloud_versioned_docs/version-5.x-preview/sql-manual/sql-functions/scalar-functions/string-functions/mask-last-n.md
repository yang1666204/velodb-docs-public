---
{
  "title": "MASK_LAST_N",
  "description": "MASKLASTN関数は、文字列の末尾N バイトをマスクするために使用されます。大文字をXに、小文字をxに置き換えます。",
  "language": "ja"
}
---
## 説明

MASK_LAST_N関数は、文字列の末尾N バイトをマスクするために使用されます。末尾N バイトにおいて、大文字を`X`、小文字を`x`、数字を`n`に置き換えます。

## 構文

```sql
MASK_LAST_N(<str>[, <n>])
```
## パラメータ

| パラメータ | 説明 |
| ------- | ----------------------------------------- |
| `<str>` | マスクする文字列。型: VARCHAR |
| `<n>` | マスクする末尾のNバイト数（オプション、デフォルトは文字列全体）。型: INT |

## 戻り値

VARCHAR型を返し、末尾のNバイトがマスクされた文字列。

特殊なケース:
- いずれかのパラメータがNULLの場合、NULLを返す
- 英数字以外の文字は変更されない
- `<n>`が文字列長より大きい場合、文字列全体をマスクする

## 例

1. 基本的な使用法: 末尾4バイトをマスク

```sql
SELECT mask_last_n('1234-5678', 4);
```
```text
+-----------------------------+
| mask_last_n('1234-5678', 4) |
+-----------------------------+
| 1234-nnnn                   |
+-----------------------------+
```
2. nを指定しない場合（文字列全体をマスクする）

```sql
SELECT mask_last_n('abc123');
```
```text
+----------------------+
| mask_last_n('abc123') |
+----------------------+
| xxxnnn               |
+----------------------+
```
3. nが文字列長を超える

```sql
SELECT mask_last_n('Hello', 100);
```
```text
+---------------------------+
| mask_last_n('Hello', 100) |
+---------------------------+
| Xxxxx                     |
+---------------------------+
```
4. NULL値の処理

```sql
SELECT mask_last_n(NULL, 5);
```
```text
+----------------------+
| mask_last_n(NULL, 5) |
+----------------------+
| NULL                 |
+----------------------+
```
5. nが0の場合（文字をマスクしない）

```sql
SELECT mask_last_n('Hello123', 0);
```
```text
+----------------------------+
| mask_last_n('Hello123', 0) |
+----------------------------+
| Hello123                   |
+----------------------------+
```
6. nが文字列長より大きい場合（文字列全体をマスクする）

```sql
SELECT mask_last_n('Test', 100);
```
```text
+--------------------------+
| mask_last_n('Test', 100) |
+--------------------------+
| Xxxx                     |
+--------------------------+
```
7. メールアドレスのドメイン部分をマスク

```sql
SELECT mask_last_n('user@example.com', 11);
```
```text
+-------------------------------------+
| mask_last_n('user@example.com', 11) |
+-------------------------------------+
| user@xxxxxxx.xxx                    |
+-------------------------------------+
```
8. 電話番号の下4桁をマスクする

```sql
SELECT mask_last_n('13812345678', 4);
```
```text
+-------------------------------+
| mask_last_n('13812345678', 4) |
+-------------------------------+
| 1381234nnnn                   |
+-------------------------------+
```
9. 英字、数字、特殊文字の混在

```sql
SELECT mask_last_n('ABC-123-xyz', 7);
```
```text
+--------------------------------+
| mask_last_n('ABC-123-xyz', 7)  |
+--------------------------------+
| ABC-nnn-xxx                    |
+--------------------------------+
```
10. UTF-8文字の処理（バイト単位でのマスク）

```sql
SELECT mask_last_n('Helloṭṛ123', 9);
```
```text
+--------------------------------+
| mask_last_n('Hello你好123', 9) |
+--------------------------------+
| Hello你好nnn                   |
+--------------------------------+
```
```sql
SELECT mask_last_n('eeeéèêëìí1234');
```
```text
+------------------------------------+
| mask_last_n('eeeéèêëìí1234')       |
+------------------------------------+
| xxxéèêëìínnnn                      |
+------------------------------------+
```
