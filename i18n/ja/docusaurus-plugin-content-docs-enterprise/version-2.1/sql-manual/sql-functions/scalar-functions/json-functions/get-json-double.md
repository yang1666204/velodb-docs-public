---
{
  "title": "GET_JSON_DOUBLE",
  "description": "Functionは、JSONドキュメントからフィールドの値を抽出し、DOUBLE型に変換するために使用されます。",
  "language": "ja"
}
---
## 説明

この関数は、JSONドキュメントからフィールドの値を抽出し、DOUBLE型に変換するために使用されます。この関数は、指定されたパス上のフィールドの値を返すか、値がDOUBLE型に変換できない場合やパスが指すフィールドが存在しない場合はNULLを返します。

## 構文

```sql
GET_JSON_DOUBLE( <json_str>, <json_path>)
```
## 必須パラメータ

| parameters| described|
|------|------|
| `<json_str>`| データを抽出するJSONストリングが必要です。 |
| `<json_path>`| JSONパス。フィールドの場所を指定します。パスはドット記法で表すことができます。 |

## 戻り値

- パスによって指し示されるフィールドのDOUBLE値を返します。
- 指定されたパスに対応するフィールドが見つからない、またはフィールド値をDOUBLE型に変換できない場合はNULLを返します。

## 使用上の注意

jsonストリング内の指定されたパスの浮動小数点コンテンツを解析して取得します。
`<json_path>`は$記号で始まり、.をパス区切り文字として使用する必要があります。パスに.が含まれる場合は、二重引用符で囲むことができます。
[ ]を使用して配列インデックスを表し、0から開始します。
pathのコンテンツには"、[、]を含めることはできません。
`<json_str>`が正しくフォーマットされていない、またはjson_pathが正しくフォーマットされていない、またはマッチが見つからない場合はNULLを返します。
さらに、同じ機能を実現するためにjsonb型とjsonb_extract_XXX関数の使用を推奨します。
特別な状況は以下のように処理されます：
- json_pathで指定されたフィールドがJSONに存在しない場合はNULLを返します
- JSONでjson_pathによって指定されたフィールドの実際の型がjson_extract_tで指定された型と一致しない場合、指定された型に無損失変換できれば指定された型tを返し、できない場合はNULLを返します。

## 例

1. キーが"k1"の値を取得

```sql
SELECT get_json_double('{"k1":1.3, "k2":"2"}', "$.k1");
```
```sql
+-------------------------------------------------+
| get_json_double('{"k1":1.3, "k2":"2"}', '$.k1') |
+-------------------------------------------------+
|                                             1.3 |
+-------------------------------------------------+
```
2. キー "my.key" を持つ配列の2番目の要素を取得します

```sql
SELECT get_json_double('{"k1":"v1", "my.key":[1.1, 2.2, 3.3]}', '$. "my.key"[1]');
```
```sql
+---------------------------------------------------------------------------+
| get_json_double('{"k1":"v1", "my.key":[1.1, 2.2, 3.3]}', '$."my.key"[1]') |
+---------------------------------------------------------------------------+
|                                                                       2.2 |
+---------------------------------------------------------------------------+
```
3. セカンダリパス k1.key -&gt; k2 を持つ配列の最初の要素を取得します。

```sql
SELECT get_json_double('{"k1.key":{"k2":[1.1, 2.2]}}', '$."k1.key".k2[0]');
```
```sql
+---------------------------------------------------------------------+
| get_json_double('{"k1.key":{"k2":[1.1, 2.2]}}', '$."k1.key".k2[0]') |
+---------------------------------------------------------------------+
|                                                                 1.1 |
+---------------------------------------------------------------------+
```
